Create a refined generative illustration titled “The city after rain” as a visual artifact inside an AI conversation.

This artifact represents StreamViz's `art` capability. Call `visualize_read_me` with type `art`, then produce the final artifact with `visualize_show_widget`.

Depict an urban water cycle as one continuous editorial cityscape after rainfall. Rain falls across rooftops, tree canopies, and streets; some water is captured by green roofs and rain gardens, some filters through permeable pavement into groundwater, some travels through stormwater infrastructure toward a living river, and treated water quietly returns to the city for reuse. The illustration should communicate how a resilient city slows, cleans, stores, and reuses water without becoming a dashboard or conventional flowchart.

Use a wide sectional composition that combines a restrained skyline above ground with soil, roots, pipes, and an aquifer below. Water should be the visual thread connecting the whole scene through repeated droplets, thin paths, and small moving markers. Include only five sparse labels: `rain`, `green roof`, `rain garden`, `aquifer`, and `reuse`. Keep infrastructure legible but secondary to the overall atmosphere. Use no gradients, glow, blur, drop shadows, or decorative noise.

Build it with self-contained HTML, CSS, and inline SVG only. Use StreamViz semantic and chart-series variables rather than hard-coded colors, support light and dark themes, include an accessible SVG title and description, honor `prefers-reduced-motion`, use no external assets, and fit an approximately 760 × 520 pixel conversation artifact without horizontal scrolling.
