Create a refined generative illustration titled “The agent constellation” as a visual artifact inside an AI conversation.

This artifact represents StreamViz's `art` capability. Call `visualize_read_me` with type `art`, then produce the final artifact with `visualize_show_widget`.

Depict a calm, abstract map of an agent forming an answer: a central reasoning core receives a user signal, sends paths through memory and tool constellations, then converges into a final response. The composition should feel technical and editorial, suitable for the hero gallery of an open-source developer package—not like a dashboard or flowchart.

Use a strong asymmetric composition with layered orbital paths, small data particles, five labeled anchors (`input`, `reasoning`, `memory`, `tools`, `response`), and subtle motion that suggests streaming tokens. Keep labels sparse and let the visual carry the idea. Use no gradients, glow, blur, or decorative noise.

Build it with self-contained HTML, CSS, and inline SVG only. Use StreamViz semantic and chart-series variables rather than hard-coded colors, support light/dark themes, include accessible SVG title and description, honor `prefers-reduced-motion`, use no external assets, and fit an approximately 760 × 520 pixel conversation artifact without horizontal scrolling.
